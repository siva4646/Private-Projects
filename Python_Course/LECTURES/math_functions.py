import math # Imports the math library

x = 2.9
print(round(x)) # Returns the variable rounded to the nearest integer
print(abs(-2.9)) # Returns the absolute value of the variable, whether it is positive or negative.
print(math.ceil(2.9)) # Returns the ceil value of the variable
print(math.floor(2.9)) # Returns the floor value of the variable
